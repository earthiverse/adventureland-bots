import { PingCompensatedCharacter } from "alclient"
import { Strategist } from "../context.js"
import { Setup } from "./base"

export function constructABTestingSetup(contexts: Strategist<PingCompensatedCharacter>[]): Setup {
    return {
        configs: [
            // TODO
        ],
    }
}

export function constructABTestingSetupHelperSetup(contexts: Strategist<PingCompensatedCharacter>[]): Setup {
    return {
        configs: [
            // TODO
        ],
    }
}
