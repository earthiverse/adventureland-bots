# Project Guidelines

- **Dependency & Package Fixes**: The user can edit local dependencies directly (e.g., `alclient`). When facing bugs or hanging promises in dependency packages, prefer diagnosing or pointing out fixes within the dependency package rather than replacing the library logic with application-level workarounds.
